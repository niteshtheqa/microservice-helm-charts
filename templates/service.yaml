apiVersion: v1
kind: Service
metadata:
  name: {{ include "microserviceschart.fullname" . }}
  labels:
    {{- include "microserviceschart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "microserviceschart.selectorLabels" . | nindent 4 }}
