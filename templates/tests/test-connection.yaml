apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "microserviceschart.fullname" . }}-test-connection"
  labels:
    {{- include "microserviceschart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "microserviceschart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
